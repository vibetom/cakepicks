# Secrets — never commit
.env
.streamlit/secrets.toml

# Local state (the hosted app keeps its copy on the parlay-data branch)
config.json
data/config.json
data/runs/*.json
!data/runs/.gitkeep
data/odds/
data/rosters/
data/projections/
data/.write-probe

# Python
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/

# OS
.DS_Store
